/*!
  * Copyright 2019 - Author - Gaurav
  */

// Note: Must be imported only once.
import 'reflect-metadata';

// tslint:disable-next-line: no-var-requires
const { addPath } = require('app-module-path');

addPath(__dirname);
